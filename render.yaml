# Render — Yii2 backend (document root: backend/web)

services:
  - type: web
    name: portfolio-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: YII_ENV
        value: prod
      - key: YII_DEBUG
        value: "0"
