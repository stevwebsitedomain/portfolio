{
    "name": "portfolio/backend-api",
    "description": "Minimal portfolio API for Render (Gmail SMTP via PHPMailer)",
    "require": {
        "php": ">=8.2",
        "phpmailer/phpmailer": "6.9"
    },
    "autoload": {
        "psr-4": {
            "Portfolio\\Api\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
